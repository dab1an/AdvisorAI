<configuration>
    <system.web>
        <!-- Increase request timeout to 300 seconds (5 minutes) -->
        <httpRuntime executionTimeout="300" maxRequestLength="4096" />
        <!-- Ensure debug is set to false for the timeout to take effect in production -->
        <compilation debug="false" />
    </system.web>
</configuration>